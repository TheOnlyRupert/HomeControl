<Page x:Class="HomeControl.Source.Modules.Calendar.Calendar" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="SliderButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderThumbStyle" TargetType="{x:Type Thumb}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border BorderThickness="0" CornerRadius="8" Background="Gray"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBar" TargetType="{x:Type ScrollBar}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto" MinWidth="{TemplateBinding MinWidth}"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TickBar Grid.Column="0" x:Name="TopTick" SnapsToDevicePixels="True" Placement="Left" Width="4" Visibility="Collapsed"/>

                <Track Grid.Column="1" x:Name="PART_Track">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource SliderButtonStyle}" Command="Slider.DecreaseLarge"/>
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource SliderThumbStyle}"/>
                    </Track.Thumb>
                </Track>

                <TickBar x:Name="BottomTick" SnapsToDevicePixels="True" Grid.Column="2" Placement="Right" Width="4" Visibility="Collapsed"/>
            </Grid>
        </ControlTemplate>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="MinWidth" Value="8"/>
                    <Setter Property="Template" Value="{StaticResource VerticalScrollBar}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Border BorderBrush="Black" BorderThickness="1" Margin="4 4 0 4">
        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="24"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" Source="../../../Resources/Images/icons/calendar.png" HorizontalAlignment="Left" Width="24" Height="24"/>

                <Button Grid.Column="1" Content="&lt;" Command="{Binding ButtonCommand}" Width="32" FontSize="18" HorizontalContentAlignment="Center" HorizontalAlignment="Right"
                        CommandParameter="subMonth">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBlock Grid.Column="2" Text="{Binding CurrentMonthAndYear}" HorizontalAlignment="Center" Foreground="White" FontSize="18"/>

                <Button Grid.Column="3" Content="&gt;" Command="{Binding ButtonCommand}" Width="32" FontSize="18" HorizontalContentAlignment="Center" HorizontalAlignment="Left"
                        CommandParameter="addMonth">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="4" Content="Today" Command="{Binding ButtonCommand}" FontSize="18" CommandParameter="today">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Sun" FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Mon" FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="Tue" FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="Wed" FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="4" Text="Thur" FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="5" Text="Fri" FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Grid.Column="6" Text="Sat" FontSize="16" Foreground="White" HorizontalAlignment="Center"/>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0" Grid.Column="0" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Foreground="White" Command="{Binding ButtonCommand}"
                        CommandParameter="button1">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background" Value="{Binding Button1BackgroundColor}"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button1Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button1HolidayText}" Foreground="YellowGreen" HorizontalAlignment="Right"/>
                        </Grid>

                        <ListView ItemsSource="{Binding Button1EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>

                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="0" Grid.Column="1" Background="{Binding Button2BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button2">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button2Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button2HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>

                        <ListView ItemsSource="{Binding Button2EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="0" Grid.Column="2" Background="{Binding Button3BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button3">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button3Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button3HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>

                        <ListView ItemsSource="{Binding Button3EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>

                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="0" Grid.Column="3" Background="{Binding Button4BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button4">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button4Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button4HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>

                        <ListView ItemsSource="{Binding Button4EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="0" Grid.Column="4" Background="{Binding Button5BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button5">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button5Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button5HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button5EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="0" Grid.Column="5" Background="{Binding Button6BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button6">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button6Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button6HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button6EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="0" Grid.Column="6" Background="{Binding Button7BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button7">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button7Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button7HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button7EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="1" Grid.Column="0" Background="{Binding Button8BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button8">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button8Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button8HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button8EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="1" Grid.Column="1" Background="{Binding Button9BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button9">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button9Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button9HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button9EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="1" Grid.Column="2" Background="{Binding Button10BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button10">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button10Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button10HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button10EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="1" Grid.Column="3" Background="{Binding Button11BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button11">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button11Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button11HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button11EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="1" Grid.Column="4" Background="{Binding Button12BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button12">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button12Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button12HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button12EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="1" Grid.Column="5" Background="{Binding Button13BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button13">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button13Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button13HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button13EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="1" Grid.Column="6" Background="{Binding Button14BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button14">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button14Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button14HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button14EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="0" Background="{Binding Button15BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button15">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button15Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button15HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button15EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="1" Background="{Binding Button16BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button16">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button16Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button16HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button16EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="2" Background="{Binding Button17BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button17">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button17Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button17HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button17EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="3" Background="{Binding Button18BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button18">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">


                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button18Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button18HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button18EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="4" Background="{Binding Button19BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button19">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button19Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button19HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button19EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="5" Background="{Binding Button20BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button20">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button20Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button20HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button20EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="2" Grid.Column="6" Background="{Binding Button21BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button21">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button21Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button21HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button21EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="3" Grid.Column="0" Background="{Binding Button22BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button22">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button22Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button22HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button22EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="3" Grid.Column="1" Background="{Binding Button23BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button23">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button23Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button23HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button23EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="3" Grid.Column="2" Background="{Binding Button24BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button24">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button24Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button24HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button24EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="3" Grid.Column="3" Background="{Binding Button25BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button25">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button25Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button25HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button25EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="3" Grid.Column="4" Background="{Binding Button26BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button26">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button26Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button26HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button26EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="3" Grid.Column="5" Background="{Binding Button27BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button27">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button27Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button27HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button27EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="3" Grid.Column="6" Background="{Binding Button28BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button28">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button28Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button28HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button28EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="4" Grid.Column="0" Background="{Binding Button29BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button29">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button29Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button29HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button29EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="4" Grid.Column="1" Background="{Binding Button30BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button30">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button30Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button30HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button30EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="4" Grid.Column="2" Background="{Binding Button31BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button31">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button31Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button31HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button31EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="4" Grid.Column="3" Background="{Binding Button32BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button32">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button32Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button32HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button32EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="4" Grid.Column="4" Background="{Binding Button33BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button33">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button33Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button33HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button33EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="4" Grid.Column="5" Background="{Binding Button34BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button34">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button34Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button34HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button34EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="4" Grid.Column="6" Background="{Binding Button35BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button35">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button35Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button35HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button35EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="5" Grid.Column="0" Background="{Binding Button36BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button36">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button36Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button36HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button36EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="5" Grid.Column="1" Background="{Binding Button37BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button37">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button37Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button37HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button37EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="5" Grid.Column="2" Background="{Binding Button38BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button38">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button38Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button38HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button38EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="5" Grid.Column="3" Background="{Binding Button39BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button39">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button39Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button39HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button39EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="5" Grid.Column="4" Background="{Binding Button40BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button40">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button40Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button40HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button40EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="5" Grid.Column="5" Background="{Binding Button41BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button41">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button41Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button41HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button41EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>

                <Button Grid.Row="5" Grid.Column="6" Background="{Binding Button42BackgroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                        Foreground="White" Command="{Binding ButtonCommand}" CommandParameter="button42">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="Black" BorderThickness="1">
                                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <DockPanel>
                        <Grid DockPanel.Dock="Top">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Button42Date}" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Button42HolidayText}" HorizontalAlignment="Right" Foreground="YellowGreen"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Button42EventList}" Foreground="White" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                  Focusable="False" FontSize="8" DockPanel.Dock="Bottom" Margin="-4 0 0 0">
                            <ListView.Resources>
                                <Style TargetType="GridViewColumnHeader">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </Style>

                                <Style TargetType="ListViewItem">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding person}" Value="1">
                                            <Setter Property="Foreground" Value="CornflowerBlue"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="2">
                                            <Setter Property="Foreground" Value="Pink"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="3">
                                            <Setter Property="Foreground" Value="Yellow"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="4">
                                            <Setter Property="Foreground" Value="White"/>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding person}" Value="5">
                                            <Setter Property="Foreground" Value="Navy"/>
                                        </DataTrigger>

                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <MultiTrigger.Setters>
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Focusable" Value="False"/>
                                            </MultiTrigger.Setters>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListView.Resources>
                            <ListView.View>
                                <GridView AllowsColumnReorder="false">
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=name}" Width="Auto"/>
                                    <GridViewColumn DisplayMemberBinding="{Binding Path=person}" Width="0"/>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </DockPanel>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Page>